{
  "name": "vuekumi-platform",
  "version": "0.1.0",
  "private": true,
  "description": "VUEKUMI stock photo platform with local backend APIs.",
  "scripts": {
    "start": "node backend/server.js",
    "check": "node --check backend/server.js && node --check backend/auth.js && node --check backend/defaults.js && node --check backend/store.js && node --check backend/platform.js && node --check vuekumi-platform.js && node --check admin.js"
  }
}
